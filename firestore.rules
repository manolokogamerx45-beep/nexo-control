rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the trusted server accesses these records using IAM credentials.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
