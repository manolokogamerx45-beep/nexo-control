server {
    listen 8080;
    server_tokens off;
    root /usr/share/nginx/html;
    client_max_body_size 16k;
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options DENY always;
    add_header Referrer-Policy no-referrer always;
    add_header Cache-Control no-store always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'" always;

    location = /health/web { access_log off; return 200 'ok'; }
    location /api/ {
        proxy_pass http://auth-api:4173;
        proxy_set_header Host $host;
        proxy_set_header Origin $http_origin;
    }
    # Never serve the operational bundle without checking the session.
    location = /app.js {
        proxy_pass http://auth-api:4173;
        proxy_set_header Host $host;
    }
    location = / { try_files /index.html =404; }
    location ~ ^/(index\.html|styles\.css|auth\.css|auth\.js)$ { try_files $uri =404; }
    location / { return 404; }
}
