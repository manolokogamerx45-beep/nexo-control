name: nexo_mobile
description: Aplicación móvil de JIDE NOVA CORE con Flutter y MVVM.
publish_to: none
version: 0.1.0+1
environment:
  sdk: ^3.12.0
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.5.0
  crypto: ^3.0.6
  flutter_secure_storage: ^9.2.4
  flutter_web_auth_2: ^5.1.0
  intl: ^0.20.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/demo_catalog.json
